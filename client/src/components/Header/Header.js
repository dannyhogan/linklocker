import React from 'react';
import Logo from '../Logo/Logo';
import './Header.scss'

const Header = () => {
  return (
    <div className="Header">
      <Logo />
    </div>
  );
}

export default Header;
